package dev.freitas.delve.game.model;

/** What the player is currently doing — drives which commands are available. */
public enum SessionState {
    IN_TOWN,
    EXPLORING,
    IN_COMBAT,
    RESTING
}
