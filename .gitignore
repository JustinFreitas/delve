# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Build artifacts copied to root
delve.jar

# H2 database files
database.mv.db
database.trace.db
*.mv.db
*.trace.db

# IDE
.idea/
*.iml
.vscode/
out/
bin/

# OS
.DS_Store
Thumbs.db

# Secrets / local overrides
delve-local.properties
*.env
# PM2 local secrets (delve.env.example is the committed template)
delve.env
!delve.env.example

# Imported module JSON is the DM's own (from their own PDFs) — keep it local, except the sample.
src/main/resources/content/modules/*.json
!src/main/resources/content/modules/sample.json
content/modules/
